# dagger-modules

A collection of reusable [Dagger](https://dagger.io/) modules

## Usage

Install any of my dagger modules as a module dependency with:

```
dagger install github.com/wouter2397/dagger-modules/<module_name>
```

